@model IEnumerable<LibrariaMVC.Models.Zhanri>

<ul id="categories">
   
        <li>@foreach (var genre in Model)
    {
        <li>@Html.ActionLink(genre.Name, "Browse", new { genre = genre.Name })</li>
    }
           
        </li>
    
</ul>